@{
    ViewData["Title"] = "Tin nhắn với " + ViewBag.PartnerName;
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["IsSettingsPage"] = true;
}

@section Styles {
    <link rel="stylesheet" href="~/css/chat-page.css" asp-append-version="true" />
}

<div class="chat-page-container">
    <div class="chat-wrapper">
        <div class="chat-header-bar">
            <a href="/Messages" class="back-btn">
                <i class="fa-solid fa-arrow-left"></i>
            </a>
            <img src="@ViewBag.PartnerAvatar" alt="@ViewBag.PartnerName" class="chat-partner-avatar">
            <div class="chat-partner-info">
                <h3>@ViewBag.PartnerName</h3>
                <span class="online-status" id="partnerStatus">
                    <span class="status-dot"></span>
                    Đang hoạt động
                </span>
            </div>
            <div class="chat-actions">
                <button class="chat-action-btn" title="Gọi thoại">
                    <i class="fa-solid fa-phone"></i>
                </button>
                <button class="chat-action-btn" title="Gọi video">
                    <i class="fa-solid fa-video"></i>
                </button>
                <button class="chat-action-btn" title="Thông tin">
                    <i class="fa-solid fa-circle-info"></i>
                </button>
            </div>
        </div>

        <div class="chat-messages-area" id="messagesArea">
            <div class="loading-messages">
                <i class="fa-solid fa-spinner fa-spin"></i>
                <p>Đang tải tin nhắn...</p>
            </div>
        </div>

        <div class="typing-indicator" id="typingIndicator" style="display: none;">
            <img src="@ViewBag.PartnerAvatar" alt="" class="typing-avatar">
            <div class="typing-dots">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>

        <div class="chat-input-bar">
            <input type="file" id="imageInput" accept="image/*" style="display: none;">
            <button class="input-action-btn" title="Ảnh" onclick="document.getElementById('imageInput').click()">
                <i class="fa-solid fa-image"></i>
            </button>
            <button class="input-action-btn" title="Sticker">
                <i class="fa-solid fa-face-grin-stars"></i>
            </button>
            <div class="message-input-wrapper">
                <input type="text" id="messageInput" placeholder="Aa" autocomplete="off">
                <button class="emoji-btn" title="Biểu tượng cảm xúc" id="emojiBtn">
                    <i class="fa-regular fa-face-smile"></i>
                </button>
            </div>
            <button class="send-btn" id="sendBtn" title="Gửi">
                <i class="fa-solid fa-paper-plane"></i>
            </button>
        </div>

        <div class="emoji-picker" id="emojiPicker" style="display: none;">
            <div class="emoji-grid">
                <span class="emoji-item">😀</span>
                <span class="emoji-item">😂</span>
                <span class="emoji-item">🥰</span>
                <span class="emoji-item">😍</span>
                <span class="emoji-item">😘</span>
                <span class="emoji-item">😊</span>
                <span class="emoji-item">🤔</span>
                <span class="emoji-item">😢</span>
                <span class="emoji-item">😭</span>
                <span class="emoji-item">😡</span>
                <span class="emoji-item">👍</span>
                <span class="emoji-item">👎</span>
                <span class="emoji-item">🖤</span>
            </div>
        </div>

        <div class="image-preview-bar" id="imagePreviewBar" style="display: none;">
            <div class="image-preview-content">
                <img id="previewImg" src="" alt="Preview">
                <button class="remove-preview" onclick="removeImagePreview()">
                    <i class="fa-solid fa-xmark"></i>
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        window.chatConfig = {
            partnerId: @ViewBag.PartnerId,
            currentUserId: @ViewBag.CurrentUserId,
            partnerAvatar: '@ViewBag.PartnerAvatar',
            partnerName: '@ViewBag.PartnerName'
        };
    </script>
    <script src="~/js/chatPage.js" asp-append-version="true"></script>
}
